# 基本方針

- このプロジェクト内のチャットは、すべて日本語での会話を標準とする。
- 文字化け対策として、テキスト系ファイルは UTF-8（BOMなし推奨）+ LF を標準とする。
- 仕様の統一方針として、`storage` を正とし `data` は使わない。
- モジュールエントリファイルは `module.php` に統一する。
- インストーラは Web 起動を正とし、CLI 起動は任意補助とする。
- モジュール配布は Git を優先し、ZIP は互換用の任意手段とする。
